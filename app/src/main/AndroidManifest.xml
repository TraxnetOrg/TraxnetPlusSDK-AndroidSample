<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ee.traxnet.plussample.android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="ee.traxnet.plussample.android.MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="ee.traxnet.plussample.android.RewardedVideoActivity"
            android:label="@string/rewarded_video" />

        <activity
            android:name="ee.traxnet.plussample.android.InterstitialActivity"
            android:label="@string/interstitial" />

        <activity
            android:name="ee.traxnet.plussample.android.NativeBannerActivity"
            android:label="@string/native_banner" />

        <activity
            android:name="ee.traxnet.plussample.android.StandardBannerActivity"
            android:label="@string/standard_banner" />

    </application>

</manifest>